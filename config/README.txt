Since the content of the config directory is just a series of sub directories,
git will ignore config when it's tracking the structure of the repo. This README
file ensures that config gets rendered in the directory hierarchy.